<!--components/song-item-v1/index.wxml-->
<view class="item" bindtap="handleItemClick" data-id="{{item.id}}">
  <image src="{{item.al.picUrl}}" mode="widthFix" style="width: 120rpx;height: 120rpx;border-radius: 14rpx;"></image>
  <view class="content">
    <view class="name">{{item.name}}</view>
    <view class="source">{{item.ar[0].name}}·{{item.al.name}}</view>
  </view>
  <view class="right">
    <image class="image" src="../../assets/images/icons/arrow-right.png"></image>
  </view>
</view>
